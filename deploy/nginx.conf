# Legacy bare-metal config (Next.js directly on host).
# Preferred production setup:
#   - Docker Compose exposes app on 127.0.0.1:8080
#   - Host Nginx uses deploy/nginx.host.conf
#
# See deploy/nginx.host.conf and ./deploy/deploy.sh
